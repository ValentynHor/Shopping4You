server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ValentynHor/Shopping4You/bla-bla
          clone-on-start: true
          default-label: master
          search-paths:
            - '{application}/{profile}'

